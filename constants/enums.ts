export enum shiftStatus {
    PENDING = "pending",
    CONFIRMED = "confirmed",
    CANCELLED = "canceled",
}

export enum Status {
  Confirmed = "confirmed",
  Pending = "pending",
  Canceled = "canceled",
  All = "all",
}